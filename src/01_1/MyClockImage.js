import logo from '../logo.svg';

function MyClockImage () {
    return(
        <header className="MyClockImage">
            <img src={logo} className="App-logo" alt="logo" />
        </header>
    );
}

export default MyClockImage;